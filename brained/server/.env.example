# Server environment file (keep this file out of source control)
# Replace the MONGO_URI value with your MongoDB Atlas connection string (include username, password, and DB name)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/<dbname>?retryWrites=true&w=majority

# JWT signing secret (generated). Keep this secret safe.
JWT_SECRET=5f3b2c9a1e6f4d8c7a9b0e1f2d3c4b5a6f7e8d9c0b1a2e3f4d5c6b7a8e9f0a1

# Server port
PORT=5000

# Frontend URL for CORS
CLIENT_URL=http://localhost:3000
